# Dependencias
node_modules/

# Next.js
.next/
out/

# TypeScript incremental cache — rompe el build de Nixpacks si se sube
*.tsbuildinfo

# Runtime data (NO versionar)
data/
auth/

# Secrets
.env
.env.local
.env*.local
.restart
secrets/

# El cerebro del bot SÍ se versiona (06-08-2026). Venía ignorado del kit, donde cada
# usuario escribe el suyo; pero este repo es la app de Arteluk y nada más, y si el
# archivo no viaja al deploy el bot se queda sin cerebro (cae al texto genérico de
# system-prompt.ts). No lleva claves: son los precios y horarios que ya están en la web.
# prompts/negocio.md

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Hoja de claves para copiar/pegar (NO subir)
ENTORNO-EASYPANEL.local.txt
